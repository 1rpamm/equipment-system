$ sudo apt-get install postgresql-9.1
$ sudo apt-get install libpq-dev
$ sudo -u postgres psql postgres
postgres=# \password postgres
*Смените пароль для пользователя postgres
postgres=# \q
$ sudo su postgres -c psql
*Имя пользователя (ode) должно совпадать с результатом команды whoami
postgres=# CREATE ROLE ode SUPERUSER LOGIN;
postgres=# \q

Modify Gemfile:
# Remove gem 'sqlite3'
gem 'pg'

Modify database.yml in app directory
development:
  adapter: postgresql
  encoding: unicode
  database: EquipmentSystem_development
  pool: 5
  timeout: 5000
  username: ode
  password:
test:
  adapter: postgresql
  encoding: unicode
  database: EquipmentSystem_test
  pool: 5
  timeout: 5000
  username: ode
  password:

* run bundle install
$ bundle install
* create databases and migrations
$ rake db:create:all
$ rake db:migrate




rake sunspot:solr:start
                  reindex
                  stop
